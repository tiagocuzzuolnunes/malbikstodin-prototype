import { SectionPage } from '../components/shared'

export default function ReikningarPage() {
  return (
    <SectionPage
      titleKey="nav.reikningar"
      descriptionKey="pages.reikningar.description"
    />
  )
}
